'use client';

import React from 'react';

import { DynamicWidget } from '@dynamic-labs/sdk-react-core';

const Home = () => {
  return (
    <main>
      <div>
        <DynamicWidget />
      </div>
    </main>
  );
};

export default Home;
